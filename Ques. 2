Write a C program that inputs values for m and n and displays the values of the Pythagorean triple generated by the following formulas.
Side1 = m² - n²
Side2 = 2mn
Hypotenuse = m² + n²  ---------------------------------------------------

#include <stdio.h>
int main() {
    int m, n;
    int side1, side2, hypotenuse;

    printf("Enter values for m and n: ");
    scanf("%d %d", &m, &n);

    side1 = (m * m) - (n * n);
    side2 = 2 * m * n;
    hypotenuse = (m * m) + (n * n);

    printf("Pythagorean Triple:\n");
    printf("Side 1: %d\n", side1);
    printf("Side 2: %d\n", side2);
    printf("Hypotenuse: %d\n", hypotenuse);

    return 0;
}


OUTPUT :-
Pythagorean Triple:
Side 1: 5
Side 2: 12
Hypotenuse: 13
